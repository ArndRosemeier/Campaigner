# 20 — Orchestration: the board

**Current state only.** History is `docs/17-DECISION-LEDGER.md` (what was
decided, and why); seams are `docs/18-ARCHITECTURE.md` (how the code works).
This file is the chief of staff's memory that outlives its own session
(`AGENTS.md` §Chief of staff): in-flight writers, unlanded branches, and the
owner's decision queue. It is **overwritten in place, never appended**, and it
must stay ONE SCREEN — a record that no longer describes the present belongs in
`docs/17` (if it decided something) or nowhere.

## The contract

1. **Record grammar: one line per fact, stable prefix, `field=value` pairs.**
   `grep '^IN-FLIGHT' docs/20-ORCHESTRATION.md` is the query, and the answer is
   one line — not a prose block to ingest.
2. **Updated in the same commit as the landing it records** (the `docs/18` §0
   rule), and **true before the CoS reports a landing to the owner**: if the
   session dies the second after that report, a successor must be able to act
   from nothing but this file, `docs/17`, and git.
3. **Every record names something checkable** — branch, worktree, session id,
   SHA, path. Prose about state rots; a claim that can be tested does not.
4. **The reconciliation is the enforcement.** No test can see branches or
   sessions; `bash scripts/board.sh` checks every claim below against git, the
   session directories and the host, and finishes with `BOARD RECONCILED` or
   `BOARD STALE`. Session start runs it before anything is dispatched.
5. **A SESSION and an IN-FLIGHT record name the MODEL** (`model=<provider>/<model>`).
   A plain `subagent`/`subagent_fork` inherits the session's route, so that is
   what it says; a worker dispatched through a `workflow` can carry a different
   one, and the record says which, because a landing's quality is only comparable
   across experiments if the model that produced it is on the record.

## Board

```
reconciled: 0513971 · 2026-09-16T07:05Z

SESSION  | cos=session-93cd9c40-6a9a-47ee-b8c4-dfee107cc1b8 | started=2026-09-15 | model=deepseek-official/deepseek-flash | note=this session; the board may name more than one live actor, each IN-FLIGHT row names its dispatcher and model, and board.sh checks only that the acting session appears in SOME SESSION record
SESSION  | cos=session-f7c658e6-c56b-444f-b956-865d5803f9b0 | started=2026-09-15 | model=unknown (the owner's experiment) | state=FINISHED (owner, 2026-09-15) | note=kept while its session log is still inside board.sh's 6h liveness window: a FINISHED actor and a STALLED one look identical from the log's mtime, which is why the registry check (`list_agents`) decides liveness, never the mtime alone


PROBE | session=a07d5fb4-9874-47e2-b580-f5b7044dd32d | dispatched_by=session-93cd9c40 | role=read-only | note=scoping the SPELLS page (row 181's follow-up): routes global-vs-campaign, the chip/peek seam to reuse, the sort/filter precedent, the chunk query shape, and the tests to amend — its report becomes the row-182 brief; it writes no files

IN-FLIGHT | row=orphan | writer=session-c8c3921a-97a6-4f44-83b7-e3accec916b4 | model=unknown | state=finished-but-unretired — last log write 12:28, no worktree | dispatched_by=session-f7c658e6 (finished) | note=an ORPHAN of the finished actor: it never used a worktree, and the main tree is clean, so no work is at risk. Its owner is gone, so nobody will retire it; a future session may remove the dead session dir, and this row is the pointer rather than a live writer

TRAP | what=dispatcher-disjointness-miss-modulePdf | how=row 187 was briefed against 177d01b while row 184's follow-up round was STILL IN FLIGHT and had already touched `src/lib/modulePdf.ts`; the writer's rebase hit a NON-docs conflict at `buildModulePdf`'s preload region (184's `spellIndexes` line against 187's deletion of the plan-narrowed `wanted` filter) and — correctly, per AGENTS §Parallel writers 3 — ABORTED and reported instead of resolving. fix=the dispatcher READ BOTH SIDES and confirmed the union is mechanically independent (one derives the systems whose spells the build resolves, the other the images it preloads), authorized it, and required a full re-gate plus a focused run that keeps row 184's `mob-spells-pdf` pin green through the union. rule=the dispatcher's file-disjointness check must also weigh writers whose slice is IN FLIGHT in the SAME file, not only the diffs already landed — the landed-diff check cannot see them
TRAP | what=two-live-actors | how=two CoS sessions ran this project at once (session-f7c658e6 landed rows 173/174 while session-93cd9c40 was still live) and the board named only one, so the other's reconciliation flagged the board stale and the two could have exceeded the writers budget or swept each other's staged work in a shared tree. As of 2026-09-15T15:20 the owner confirms the other actor is FINISHED; only session-93cd9c40 is live | check=every dispatch names `dispatched_by` and `model` in its IN-FLIGHT row; a live writer is bounded by the project rule (≤2) ACROSS actors, and the acting CoS checks `list_agents`/worktrees before dispatching; a session with no writer of its own says so instead of assuming the budget is free
TRAP | what=stale-queue | how=this board's first AWAITING-OWNER list was COPIED from the predecessor's compaction summary and three of its four items were ALREADY BUILT and deployed (claimA-pdf = docs/17 row 157, claimA-app = row 158 with the owner's verbatim "Yes — render tables in the app as well", claimB-roster = row 159); two writers were nearly dispatched to rebuild shipped work | check=re-derive the queue from the TREE at brief time — docs/18 §5 (known debt at HEAD) + the arc docs' build order — and verify each item against HEAD before writing a brief; a pending list in a summary is a HINT, never a queue
TRAP | what=stale-local-main | how=a successor read row 167 as "complete-unlanded" while origin/main already carried AND deployed it: the writer pushed, local main stayed 5 commits behind, and `git log main..branch` was asked instead of `HEAD..origin/main` | check=scripts/board.sh §git — it now names the behind-count

QUEUE | source=OWNER REQUEST (2026-09-16, verbatim: "Lets make the model picker UI a global widget then, shared across all model picking instances (setup too)") | row RESERVED 199 | ONE MODEL-PICKING WIDGET, EVERY INSTANCE. MEASURED POPULATION at b1466a0: `features/settings/model-input.ModelInput` — 5 mounts in `settings-section.tsx` (chat / fallback / image / embedding / the other model field), 1 in `persona-section.tsx` (a persona override), 1 in `idea-board/IdeaBoardPage.tsx`, 1 in `modules/canvas/ChatSidebar.tsx` (dense: it passes 44px touch-target class props); PLUS row 193's `features/settings/model-picker.ModelPicker` in `app/layout/TopBar.tsx`. The setup wizard (`features/onboarding/SetupWizardDialog.tsx`, steps in `onboardingContent.ts`) carries NO model field today — its 'Connect your OpenRouter key' step tells the owner to go to Settings, so 'setup too' means the wizard gains the choice through the SAME widget. SCOPE: fold `ModelInput` and `ModelPicker` into ONE widget (the row-193 option seam `features/settings/model-options.listModelIds` stays the ONE account-model source) with (a) a SURFACE variant — an inline form field (label + input + browse, the `ModelInput` shape and its size/class props, so the dense canvas-chat instance is unchanged in layout) and the popover TRIGGER shape the top bar uses; (b) an OPTIONAL recents source, because recents are the GLOBAL CHAT model's (`settings.recentChatModels`, cap 8) — shown where the instance edits `defaultChatModel` (top bar, Settings' chat field, the wizard, the canvas chat sidebar), and deliberately ABSENT from the persona/image/embedding/fallback instances, where showing chat recents would be a lie about another tier; (c) free-form entry and the loud no-key/failed-fetch states kept in EVERY variant (row 193's pins); (d) the wizard presents the model choice after a working key (writing `defaultChatModel`) instead of only linking to Settings. PINS: extend row 193's `tests/architecture/one-model-option-source.test.ts` with the ONE-WIDGET population (a source scan naming every mount site, so a future copy reds), plus per-variant behaviour pins (field shape, trigger shape, recents present/absent by instance, free-form, loud states) and the wizard path writing the setting. NOTHING else changes — no new option source, no second recents store, no settings-field semantics moved. DISPATCH ORDER: 196 → 197 → 199 → 195 → 194 → 192 → 198
IN-FLIGHT | row=192 | branch=feat/act-flake | worktree=/tmp/campaigner-act-flake | base=9d03eeb | writer=c636d680-aee0-4bf6-8a2d-479974dc3952 | dispatched_by=session-93cd9c40 | model=deepseek-official/deepseek-flash | state=running | note=THE canvas-module-actions ACT-NOISE FLAKE — the docs record it as CURED (actDrained, 9/9 under a delay injection) yet a PARALLEL gate red on it once (console-noise guard, 36 act entries) while the same tree was green sequentially and in isolation, and the dispatcher's own gate never hit it; row 196 then repaired a SIBLING instance (async live-query text asserted right after findByTestId in the model-picker test, one waitFor). THE BRIEF: reproduce DETERMINISTICALLY with the documented delay-the-cause method under the gate's own two-chunk parallelism (NO synthetic load — AGENTS §Host hygiene 2 forbids it), decide which of four cases holds (the cure is incomplete / a second leak site / the guard's window too wide for a legitimate settle / not reproducible), fix at the SOURCE of the leak or barrier — never at the assertion, never by relaxing the console-noise guard or adding an ALLOWED_NOISE entry as a cure — and if the same live-query-after-find pattern exists elsewhere, fold it onto ONE shared helper rather than a third hand-rolled wait. Pins: a DETERMINISTIC pin (the un-cured form FAILS and the cured form PASSES under the injection, repeated, with raw logs and file hashes); if it is genuinely not reproducible the deliverable is the EVIDENCE recorded beside the existing docs/08 section, not a code change
IN-FLIGHT | row=194 | branch=feat/dnd5e-spells | worktree=/tmp/campaigner-dnd5e-spells | base=62f4f99 | writer=71525952-ac7b-4481-a708-e86154bbb802 | dispatched_by=session-93cd9c40 | model=deepseek-official/deepseek-flash | state=running | note=D&D 5e SPELL LANE (owner: "d&d spell lane is a go") — source `foundryvtt/dnd5e` `packs/_source/spells/{cantrip,1st-level…9th-level}` at 6.0.x, CC-BY-4.0, the SAME upstream the repo already imports SRD creatures from (docs/12:79), so no new source or licence and attribution follows the existing adapter. PIECES: (a) the spell IMPORT lane emitting row-181 `spellData` payloads, with an HONEST filter axis for 5e (never invented PF2E traditions) and the Spells page saying which axis it is; (b) 5e upcasting is PROSE ('At Higher Levels') over `system.damage.parts` (structured only where the source states it) — it does NOT reuse `spellAtRank` and never invents a number from prose (row 183's marker discipline); (c) THE TRAP, pinned by name: our cantrip auto-heighten `clamp(ceil(casterLevel/2),1,10)` is PF2E's rule and 5e cantrips scale at character levels 5/11/17, so the resolver must become SYSTEM-AWARE and apply the PF2E cantrip arm only to PF2E; (d) the library-mob half row 189 deliberately skipped for lack of a corpus: a dnd5e creature's own per-level spell list stamps `statBlockSchema.spells` with the LEVEL AS THE CAST RANK under the same no-invention policy; (e) the page's 'no 5e lane' empty state retires. BOUNDARY: `src/llm/` is row 200's this round — row 194 must not edit it, and any need to is a STOP-and-report. Fixtures must be REAL upstream documents with sha256/path/bytes recorded, never synthesised
QUEUE | source=row 193's landing (a scope boundary it stated rather than hid) | row RESERVED 198 (LOW priority, owner's call) | MAKE 'USED' COMPLETE FOR THE MODEL PICKER'S RECENTS LIST: row 193 records the global first-try model at exactly two sites (the picker's own choose, and `runEngine.executeFrom` when `resolveChatModel(...) === settings.defaultChatModel`), but module generation (`llm/moduleGen.ts`), canvas chat and the Idea Board resolve the chat model OUTSIDE the run engine, so a model SET IN SETTINGS (rather than picked in the top bar) and used only there never appears in recents. SCOPE IF THE OWNER WANTS IT: find every remaining global-model resolution site (grep for `resolveChatModel`/`defaultChatModel` consumers that start a model call), route them through the ONE `db/settingsRepo.recordRecentChatModel` seam (never a second recording path), fire-and-forget like the existing engine call (awaiting it held a run row's status long enough for a racing approve to re-enter the same step — row 193's own caught bug), and extend the differential pin so every site is enumerated by a source scan rather than by memory. NOT DISPATCHED: judged low value against the queue (196 → 197 → 195 → 194 → 192) because anything the owner PICKS already lands in the list
QUEUE | source=OWNER DECISION (2026-09-16, verbatim: "d&d spell lane is a go.") | row RESERVED 194 | THE D&D 5e SPELL LANE. FEASIBILITY ALREADY RESEARCHED AND RECORDED (docs/20 SPELLS ARC note + docs/12:79): `foundryvtt/dnd5e` ships machine-readable spells at `packs/_source/spells/{cantrip,1st-level…9th-level}` (ref 6.0.x), CC-BY-4.0 — the SAME upstream the repo already imports SRD creatures from, so no new source and no new licence, and attribution storage follows the existing dnd5e-foundry adapter's pattern. SCOPE: (a) a dnd5e SPELL import lane emitting `spell` chunks with the row-181 `spellData` payload (rank, cantrip, traditions→ n/a for 5e: use school/class lists as the filter data), so the campaign-scoped Spells page shows 5e spells for a dnd5e campaign; (b) UPCASTING IS PROSE in 5e ('At Higher Levels') over `system.damage.parts`, so it does NOT reuse `spellAtRank` — it needs its own upcast handling with the same LOUD-prose-only discipline row 183 established (never invent a number, print the source's sentence verbatim with a marker); (c) the library-mob half (row 189 deliberately left dnd5e untouched because every name would have rendered unresolved): with a 5e corpus in place, a dnd5e creature's own `system.spells` names can be stamped through the SAME `statBlockSchema.spells` field under the same no-invention policy; (d) the Spells page's per-system empty state must stop saying 'no 5e lane' once it exists. NOT DISPATCHED — the two-writer limit is full (191, 193); dispatch when a slot frees
QUEUE | source=row 189's landing (a NAMED gap it refused to fix inside its own brief, with the evidence already in the docs) | row RESERVED 191 | FOCUS SPELLS ARE AUTO-HEIGHTENED LIKE CANTRIPS, AND OUR RESOLVER DOES NOT KNOW IT. Upstream `SpellPF2e.rank` uses `isAutoHeightened = isCantrip || isFocusSpell` and derives `ceil(actor.level / 2)` for BOTH; our importer correctly stamps only the source's STATED rank, and `domain/mobSpells.mobSpellChips`'s auto-heightening arm is cantrip-only — so a focus chip prints the BASE rank and its heightened values are computed at the wrong rank. MEASURED EVIDENCE: Lawbringer Warpriest (level 5) 'Athletic Rush' — `level.value` 1, no `heightenedLevel`, entry `autoHeightenLevel: null`, upstream rank 3. `domain/spellHeightening.spellAtRank` already owns the cantrip provenance (`'cantrip-auto'`); the fix is an arm for a focus spell (a new named provenance, not a silent reuse of the cantrip one) plus the resolver passing the caster level — with a differential (level 5 vs level 9) proving the caller never computes the rank. Fixture material already exists in the recorded evidence
QUEUE | source=row 184's landing report (STANDARD/LIBRARY-MOB HALF of the spells arc; owner decision "no invented spells" governs it) | row RESERVED 189, queued behind 187/188 | WHAT IS LEFT, measured: an AI-authored mob's spells are landed (`statBlockSchema.spells`, resolved by `domain/mobSpells.mobSpellChips`), but an IMPORTED/library mob's stat block carries no `spells` at all — `src/ingest/packs/pf2e-rules.ts` (the ONE pack adapter) does not stamp the field, so a bestiary creature that casts spells prints none. The schema already carries the field, so the moment the importer stamps it the chips render everywhere with NO renderer work. THE DECISION THE BRIEF MUST MAKE FIRST: the mapping source in the pack's own data (the creature's spellcasting entries / spell list) and the same-name resolution against the campaign's imported spell corpus — an importer-written name that does not resolve follows the SAME loud-unresolved policy as the AI path (stored + unresolved chip + named issue), never dropped and never prose. Plus: a pack RE-IMPORT is the delivery mechanism (no migration — row 181's decision stands), importer-half tests, and the docs/12 source row
QUEUE | source=OWNER REPORT follow-up (module PDF, verbatim: "Ideally important NPCs should be introduced in a sidebar where the story introduces them. I understand that the sidebar can get crowded though, thats where an LLM needs to make an intelligent judgement call.") | row RESERVED 188, queued behind 186/187 | THE GAP, measured at 6002e87: a plan SECTION names exactly ONE source (`domain/documentPlan.ts`), and the block's DETAIL companion is derived from that SAME source (`modulePdf.plannedSectionBlock` → `roleDetail(section.role, artifact)`), so an NPC can only get a sidebar where the NPC's OWN prose runs — it cannot be introduced beside a PART's story text, which is what the owner is describing. MECHANISM (owner delegated the crowding judgement to the model, so the model chooses WHICH introductions deserve a sidebar while the page model still owns WHERE it fits): `documentPlanSectionSchema` gains an optional companion reference (`nullish`, so every stored plan keeps applying — no migration), validated by `documentPlanIssues` like every other reference (an id that is not in the pool is a LOUD named failure, never a guess); `llm/modulePlan.ts`'s prompt states that a section MAY name ONE companion row whose profile prints in that section's sidebar, that the sidebar is SCARCE, and that choosing which introductions earn one is the planner's judgement; `modulePdf` builds that companion through the SAME `companionOnce`/`roleDetail`/`detailPlacement` seam (never a second companion mechanism), so the §5 ladder still decides overflow. The plan dialog must DISPLAY the companion (a decision nobody can see is a decision nobody can correct). CROSS-SLICE: row 187's rule (an artifact's own image prints where the artifact is described) governs the portrait INSIDE that sidebar introduction, at the sidebar's size — 187 lands first, so 188 must consume its seam rather than re-deciding images
QUEUE | source=OWNER REPORT (module PDF, verbatim: "Some pages have just a sidebar, nothing else… If there is nothing else, of course the sidebar can use all room." / "Similar problem with main area. If there IS no sidebar, use all room" / "Some pages just say \"x has its own page, following this one\". Which is comical. A whole empty page to announce the following." / "Most of the images in the module are not actually used… i would at least expect NPCs and locations when they are described anyways.") | rows RESERVED 186 (layout) + 187 (images), both behind the 2-writer limit (184, 185 in flight) — DISPATCH 186 FIRST | DIAGNOSIS, measured at 5d7da95, so a successor does not re-derive it: (1)+(2) are ONE bug in two directions — `lib/modulePdf.pageNodes` (~:1673) emits a full-width `stack` only when `page.sidebar.length === 0`; a page whose MAIN is empty still gets the two-column frame, so the populated column is squeezed into 60mm/104mm with the rest blank (and a sidebar holding nothing but a pointer sentence squeezes the text the same way). RULE TO IMPLEMENT: a page lays out as ONE full-width column unless BOTH a real text column and a real companion column have content; marker sentences ride the TEXT column. (3) the comical page's mechanism, exactly: `lib/pdfPageModel.paginateDocument` (~:475-485) pushes `marker(ownPageNote(name))` into the sidebar and THEN calls `breakPage()` — and because a chapter-start block carries `breakBefore`, the preceding flush already left `main` EMPTY, so `flush()` pushes `{main: [], sidebar: ["…has its own page, following this one."]}` as a PAGE, and the artifact's own page follows it. A pointer must never be able to form a page: it is printed only where the page already carries text, and it never gets a column of its own. (4) ROOT CAUSE IS A REGRESSION, not an omission: the PROCEDURAL path prints every artifact's own cover (`artifactBlock(..., covers: true)` at ~:2156) but the PLANNED path — i.e. every real export since row 139 — prints ONLY the images the LLM's plan anchored (`plannedSectionBlock` ~:1590 `section.images`), and `hasImage` is `section.images.length > 0` (~:1616); the NPC gallery explicitly passes `covers: false` (~:2177). So an NPC's or location's own image prints only if the model happened to anchor it, which it usually does not. RULE TO IMPLEMENT: an artifact's OWN image prints wherever that artifact is described (its section AND the gallery), for at least NPCs and locations — the plan's `images` stays for ANCHORED extras, never as the gate for the artifact's own cover; an image that exists but cannot be embedded stays LOUD (`artifactCoverContent`), and an artifact with no image still prints nothing (owner decision, row 108). Both slices must move `tests/lib/pdfLayoutBaseline.json` honestly and keep the byte-determinism pins green; nothing in the page model may CLIP content (docs/19 §9)
QUEUE | source=docs/18 §5 known debt (SPELLS ARC: payload (181) + page (182) + heightening rule (183) are LANDED; ONLY the mob half remains — row 184 in flight for AI-authored mobs (assign library spells, chips in the description, cast-rank/heightened values via spellAtRank), with the standard/library-mob text→chip half queued behind it) (D&D 5e SPELL LANE IS FEASIBLE AND PRE-LICENSED, researched 2026-09-16: foundryvtt/dnd5e `packs/_source/spells/{cantrip,1st-level…9th-level}` at ref 6.0.x — the SAME upstream the repo already imports SRD creatures from (docs/12:79, CC-BY-4.0, attribution stored), so no new source or licence is needed; note 5e upcasting is PROSE ("At Higher Levels") with `system.damage.parts`, so it does NOT reuse the PF2E heightening computation and needs its own upcast handling. Queued after the PF2E arc) (SPELLS ARC opened by the owner: row 181 = structured spell payload in flight, now ALSO capturing heightening so packs are re-imported once; slice A = the Spells page, scoped and waiting on row 181; QUEUED NEXT = MOB SPELLS: AI-authored mobs may carry spells as chips, heightened ranks (cantrips auto-heighten), and imported mobs whose spell names resolve in the spell list get chips too), each verified at HEAD + docs/19 §11's remaining build order + the row-172 duplicate baseline | note=THE TEST-SUITE ARC: row 175 is IN (the owner's "too many tests" was a WALL-CLOCK problem: vitest 706.6s → 386s, the gate ~13min → 7m41s, nothing dropped) and row 176 is IN (the owner's test-clustering idea CONFIRMED by pilot: −1.92s per removed file, 32 test names and 116 `expect()` sites intact, test count unchanged at 3978). NEXT, in order: (a) the FLAKE hygiene IS DONE (row 178: the act() flake closed at the harness seam; note the CoS's own void-probe incident, now a rule); (b) extend the row-172 duplicate detector to `tests/` — the count half of the owner's original question — and fold `parseFile` x7 (the tripwire's biggest find). WAS: the SWEEP IS DONE AND STOPPED (row 177) — measured to its limit: framework -40.4s but gate wall only -9s, so more merging is not worth the risk; and — the `act()` console-noise family cost two 12-minute gate re-runs today (one of them mine), so fix the leaking chain at the source and give the settle-inside-`act` pattern ONE helper instead of a copy per file; and (c) extend the row-172 duplicate detector to `tests/`, since a copy-pasted test body is the same defect one layer up). THE TRIPWIRE PAID OFF ON ITS FIRST DAY: its 16-group baseline became the measured inventory of one-idea-written-many-times debt, the biggest single item being `parseFile` ×7 in the same pack adapters — the identical parse-and-map loop behind the seven `isRecord` copies row 171 folded (fold seam: one shared adapter driver). Others, each with its reason in the baseline: `workerCount` ×3 (image-queue trio), `Missing*` panels ×3, `titleCase` ×3, `on()` ×3, `Field` ×2, `getChunkByContentHash` ×2, `levelDistance`/`duplicatedAcrossBooks`/`extensionOf` ×2 each, four same-file pairs, and `settledDetail` ×3 one char under the floor (recorded in docs/18 §5). `lib/pdfExport.statBlockSection` is still the un-briefed slice (its ~20 `labelValue` call sites must be scoped first); docs/19 §11 slice 6 (print refinement) is owner-DEFERRED; the Advanced floor editor's minimum and the Cartographer's brief parity are recorded debts with a stated reason not to touch them

DECIDED-OWNER | id=mob-spells-invented | decision=NO — the AI may not invent spells; it assigns only spells that resolve in the imported library, and anything unknown is a LOUD named issue plus an unresolved chip (owner: "Lets not invent spells, i agree.")
DECIDED-OWNER | id=mob-spells-heightened | decision=COMPLETE heightening via the Paizo rule (owner: "heightening needs to be supported completely. Use the paizo formula for pathfinder."): the computation is its own slice after row 181 — `fixed` = the highest `heightening.levels[L]` with L <= cast rank (base below the first level); `interval` = apply the (+N) increment ((castRank - baseRank) / interval); CANTrips auto-heighten to max(1, ceil(level/2)); every computed value comes from the source's own structure, and a spell whose heightening is prose-only renders the applicable entry VERBATIM plus a loud 'no structured values' marker — never an invented number. Canonical fixture evidence: foundryvtt/pf2e v14-dev `packs/pf2e/spells/spells/cantrip/acid-splash.json` carries `heightening: {type: 'fixed', levels: {3,5,7,9}}` with full damage formulas


AWAITING-OWNER | id=claimB-pack | cost=1 arc, verified unwanted-until-said | question=pack ITEM entries other than feat/weapon are still dropped at ingest (absent from text/contentHash/search; verified at HEAD, `dnd5e-foundry.ts:806` / `pf2e-foundry.ts:257` try two schemas only) — the lane split (docs/17 row 14) says an arc, not a slice

LANDED | row=189 | sha=25b97f3 | verify=MY OWN: gate GREEN 322/322 files · 4156 tests · combined peak 2376MB of the 3000MB cap (raw log /tmp/gate-verify-189.out; the documented canvas-module-actions load flake did NOT reproduce in my run) PLUS my own injection with every hash printed: A untouched GREEN 3 files / 26 tests (pf2e-foundry 84a0fbbe65bb08d4da9d1c8bff0fd1bcd9bffe38, spellData 304c7579fbb7a2b44f6d9eb3b38a82d3978abab6); B (first attempt) my own no-op probe — the script only PRINTED candidate lines and changed no bytes → recorded as a VOID probe, not evidence; B2 the RATIFIED rank expression reverted to the bare base rank — pf2e-foundry 93c4166ee98cca2b7a92d9f632e9a1ef775d4a0c → RED 2 incl. the NAMED pin 'reads a ranked spell's CAST rank from the source's own heightened entry (docs/17 row 189)'; C a CANTRIP stamped WITH a cast rank — 4bb08f37400ea7530cb20e409e0204b427ce0924 → RED 3 incl. 'stamps the source name and rank VERBATIM — never normalized, defaulted or invented'; E the source's own spell NAME lower-cased — 3bdef8a91722c8f80518a0eba6f7a69fc9cab658 → RED 4 incl. 'renders an IMPORTED library mob's own spells through the ONE chips path (docs/17 row 189)' — the owner-visible end-to-end outcome is pinned; restored byte-identically after every real arm, tree clean | retired=branch feat/pack-spells (tip 25b97f3, 0 commits outside main) + worktree /tmp/campaigner-pack-spells + writer session, after this verification | note=THE MAPPING: a creature's OWN embedded `spell` items become `spells` = `{ name (source's own spelling VERBATIM), castRank = system.location.heightenedLevel ?? system.level.value }` — that expression IS upstream Foundry PF2e's `SpellPF2e.rank` (`src/module/item/spell/document.ts` @ v14-dev: `Math.clamp(location.heightenedLevel || baseRank, 1, 10)`), and a CANTRIP carries NO cast rank because upstream auto-derives `ceil(actor.level/2)` and ignores the field — the rank stays `spellAtRank`'s to derive (rows 183/184). A `spellcastingEntry` container and a carried `weapon`/`consumable` that merely embeds a spell are NOT stamped; a malformed `spell` item FAILS the creature LOUDLY rather than being dropped as equipment. A creature with no spell items OMITS the key (never `[]`) — row 184's nullish 'no field vs authored empty' split is preserved. dnd5e UNTOUCHED deliberately (no 5e spell corpus exists, so every name would render unresolved — noise, not information). COPIES 3→1: `domain/spellData.spellTraitsAreCantrip` is the ONE PF2e cantrip signal (two hand-spelled `.includes('cantrip')` in pf2e-rules.ts folded; the bestiary adapter reads it too), held by the SOURCE SCAN tests/architecture/one-cantrip-signal.test.ts. FIXTURE: tests/fixtures/packs/pf2e/ghost-mage.json = the REAL upstream document BYTE-FOR-BYTE (foundryvtt/pf2e@v14-dev packs/pf2e/pathfinder-monster-core/ghost-mage.json, sha256 b1202c2fa6e9e8f1e70073f25b94778f7b01a463ef860876441e106298cc3401, 57959 bytes) — nothing trimmed, nothing invented. MY BRIEF WAS WRONG AND THE WRITER PROVED IT (BLOCKED, then ratified): I pinned `castRank = system.level.value`; the real corpus differs (Ghost Mage 'Dispel Magic' base 2, heightened 3) so the chips would have shown the WRONG rank. The dispatcher verified BOTH halves independently before ratifying (parsed the fixture; fetched upstream document.ts and read the `rank` getter). TWO CORRECTIONS TO MY OWN RATIFICATION MESSAGE, also measured: the fixture has FIVE cantrips (not four) and their `location` carries NO `heightenedLevel` key at all; and a NEW GAP it recorded rather than silently fixing — upstream auto-heightens FOCUS spells the same way (`isAutoHeightened = isCantrip || isFocusSpell`) but a focus item may state no rank anywhere (Lawbringer Warpriest, level 5, 'Athletic Rush': level 1, no heightenedLevel, entry autoHeightenLevel null, upstream rank 3), while `domain/mobSpells.mobSpellChips`'s auto arm is cantrip-only, so a focus chip can print the base rank — queued as row 191 (that resolver was forbidden to this brief) | docs=docs/17 row 189 (ratification + the focus follow-up); docs/12 §5 mapping row + §9 (the v1 'no spellcasting data' cut marked SUPERSEDED, not deleted); docs/11; docs/18 §2.1 row 105; docs/08-TESTING (matrix section + the foundry-pf2e lane digest/count 1→2)
LANDED | row=200 | sha=9d03eeb | verify=MY OWN: gate GREEN 331/331 files · 4269 tests · combined peak 2344MB of the 3000MB cap (raw log /tmp/gate-verify-200.out) PLUS my own injection with every hash printed: A untouched GREEN 3 files / 15 tests (runEngine 68247373007a27611f522b68da7d586b033ab18e, mobSpellPrompt 3a2b305178c614475fbc2305bf8de0a6e4ce29e2 — BYTE-IDENTICAL to the hashes the writer reported as its own baseline); B the contract clause's composer made to answer null — mobSpellPrompt b89701f8866b7a89d55969d3e0f7b504a1e506e3 → RED 3 = the THREE-LANE differential the brief demanded, one red per lane ('the NPC stat-block step carries the clause and the vocabulary with a corpus', 'the encounter draft …', 'the Cartographer lane …'); C my own first attempt was a VOID probe (I guessed the corpus-gate function's name and it is NOT exported; the injection threw, no bytes changed → recorded as VOID, not evidence); C2 the ONE corpus gate forced true — mobSpellPrompt ae2d5f2b7abd3d6c87674235f47af4a80fec5aee → RED 3 = exactly the absent-arm pins ('the NPC stat-block step keeps its PRE-ARC bytes without a corpus', 'the encounter draft carries neither without a corpus', 'the Cartographer lane carries neither without a corpus'); restored byte-identically after every real arm, tree clean | retired=branch feat/caster-contract (tip 9d03eeb) + worktree /tmp/campaigner-caster-contract + writer session, after this verification | note=THE OWNER'S CASTER GAP IS CLOSED IN THE TWO PLACES THE PROBE FOUND. (1) The contradiction is gone: the spells ENTRY SHAPE is `MOB_SPELL_ENTRY_SHAPE` (`llm/promptScaffolding`), rendered by BOTH prompt halves through ONE composer (`formatMobSpellSection` + `formatMobSpellContractClause` in `llm/mobSpellPrompt`) behind ONE corpus gate (`mobSpellVocabularyRenders`) — so the contract now NAMES `spells` exactly when a library exists, and a spell-less system keeps its PRE-ARC BYTES (my arm C2 is that pin). Pinned by the NEW `tests/architecture/one-spells-shape.test.ts` ('castRank' appears in exactly ONE src file). (2) THE CARTOGRAPHER LANE IS WIRED — the lane that stocks a dungeon, i.e. the owner's own case: `runEncounterBrief` reads the ONE library, renders the vocabulary, names `spells` in its inline statBlock shape, and runs `encounterSpellIssues` + ONE `formatMobSpellRepair` turn + the raw `spellIssues`/amber notice (the entry stays on the block, so finalize persists it and the chip renders UNRESOLVED); validation runs with or without a corpus. Proven end-to-end by the new `tests/llm/mob-spells-lanes.test.ts` (real engine, mocked chat). DRAFT-SIGNAL DECISION: NOT added, as a NAMED boundary — `buildEntityBrief` already renders the module's own paragraphs + premise into `input.brief`, which the NPC draft AND the stat-block step receive, so a caster identity stated in the module text already reaches the prompt; a nullable draft field would change the NPC draft prompt for EVERY campaign (spell-less included) for a signal the brief carries. Recorded as a possible follow-up (the entity `intent` row-141 precedent). WRITER NOTES: the expected docs conflict did NOT occur (git auto-merged disjoint regions; `git diff --name-only 4a2d5bc HEAD` names only its 11 files); the clause can only render where an inline stat-block shape hint renders (with CITED library blocks there is no authored block to carry spells — by design, the vocabulary still renders); and `tests/llm/rejectionReason.test.ts`'s exactly-N source scan was HONESTLY AMENDED 7→8 deciding sites for the Cartographer's spell-repair refusal, named in row 200 and docs/08. THE OWNER'S REMAINING ACTION (unchanged, and not code): a campaign whose rules pack was imported BEFORE the spells arc keeps `section` chunks with no payload, so its corpus is empty and nothing renders — it needs a RE-IMPORT (docs/12:1096); the Spells page is the check | docs=docs/17 row 200; docs/11 §Mob spells (the composer + the Cartographer as the THIRD spell-carrying lane); docs/18 §2 row 211 (the composer seam + the forbidden second shape); docs/08-TESTING (new row-200 matrix section)
LANDED | row=195 | sha=62f4f99 | verify=MY OWN: gate GREEN 329/329 files · 4258 tests · combined peak 2309MB of the 3000MB cap (raw log /tmp/gate-verify-195.out) PLUS my own injection with every hash printed: A untouched GREEN 3 files / 19 tests (module-restock 0eb5bd8e31d8163bfa96919410fb4ef444da34d1, artifact-editor a713274a5733cf9cda83435dca2b0f0a51013d09 — BYTE-IDENTICAL to the hashes the writer reported as its own baseline); B the sweep's STOP check before each encounter removed — module-restock b43f9a810d303532510f4a816385021ea5352b4c → RED 1 on the NAMED pin 'a Stop all ends the sweep at the next boundary and says so'; C the per-encounter repopulate call removed — module-restock b4d0f82d954f0ab464d19cde49417b856a66f507 → RED 6 incl. 'visits EVERY encounter the module owns, never another module's or a campaign-level one', 'runs SEQUENTIALLY, holds the module slot throughout, and reports through the dock', 'is LOUD and names every failure with its reason (continue-on-failure policy)', 'a run cancelled by the existing Stop is a stop, never a named failure' and 'pressing it runs the real sweep'; restored byte-identically, tree clean | retired=branch feat/difficulty-restock (tip 62f4f99) + worktree /tmp/campaigner-difficulty-restock + writer session, after this verification | note=COPIES 2→1: `features/modules/module-difficulty-control.ModuleDifficultyControl` is the ONE five-step/label renderer, mounted by the New Module dialog AND the artifact editor (grepped `MODULE_DIFFICULTIES.map(`, `<ModuleDifficultyControl`) — the owner's 'add that difficulty selector from module creation there' is literally the same component in both places. The editor's control writes `module.difficulty` through `db/moduleRepo.patchModule` (a REAL-Dexie pin) and sits beside `encounter-repopulate`; a campaign-level encounter shows the no-module state instead. The module action `ModuleRestockButton` (with a read-only difficulty badge) is mounted on the canvas header AND the campaign-tree module row (the ModulePdfButton precedent), and the sweep (`features/modules/module-restock.ts`) drives the EXISTING `encounterRegen.repopulateEncounter` sequentially, holds the module generation slot, reports through the existing dock, and inherits row 196's portrait trigger (pinned by a portrait-queues integration row). FAILURE POLICY, stated by the writer: CONTINUE and then report EVERY failure by name (a stop-on-first-failure would guarantee a half-done module for no gain); the report is raised BY THE SWEEP (`toastErrorPersistent` naming each encounter + reason, plus a pasteable `[campaigner] module-restock summary` record) so a half-restocked module can never look complete. Stop reuses the EXISTING Stop-all epoch (a cancelled run is a stop, never a failure) — no second cancel mechanism. HONEST LIMITS, documented: repopulate is ROSTER-ONLY (rooms, layout and map preserved) and does NOT rewrite the module's prose — the per-artifact 'also redesign name and prose' checkbox remains the lever; the felt change is the fights, not the words. MY BRIEF'S ENGINE COORDINATES WERE STALE (`runEngine.ts:3954-3962` → the fresh read is now `:4066-4078`) and no engine work was needed, exactly as the brief predicted; `patchModule` was the existing update path | docs=docs/17 row 195; docs/08-MODULE-DESIGNER (Creation UI + a new post-creation section); docs/11 D18 amendment (the sweep is a new caller of the ONE seam); docs/18 §2.3; docs/08-TESTING (new section + the updated row-190 matrix)
LANDED | row=199 | sha=f4f21c0 | verify=MY OWN: gate GREEN 328/328 files · 4244 tests · combined peak 2277MB of the 3000MB cap (raw log /tmp/gate-verify-199.out) PLUS my own injection with every hash printed: A untouched GREEN 4 files / 77 tests (TopBar 62235ce1529ee5e8c2ee41990b10bbf6af200e53, SetupWizardDialog 49c456cb5bd1e767d594d06cd6aaaa9737f2d9e1 — the wizard hash byte-identical to the writer's reported baseline); B the TOP-BAR mount loses its recents — TopBar daff911b6236458d279986ce44a9225026ec13d8 → RED 1 on the NAMED pin 'offers the GLOBAL chat recents only at the mounts that edit the global chat model'; C the wizard field stops writing the setting — SetupWizardDialog dda97fc3a5d4ae4fe81af731c1de63e737fc7b4f → RED 1 on 'renders the field on the OpenRouter step and it writes defaultChatModel'; restored byte-identically, tree clean | retired=branch feat/model-widget (tip f4f21c0) + worktree /tmp/campaigner-model-widget + writer session, after this verification | note=COPIES 2→1: `features/settings/model-widget.ModelWidget` is the ONE model-picking widget — `model-input.tsx` and `model-picker.tsx` are DELETED — carrying the input and trigger variants, the ONE option fetch (`listModelIds` + the image `fetchOptions` override), free-form entry, the loud no-key/failed-fetch states and the optional recents; 9 instances became 10 mounts (the wizard gained one) with every mount pinned by the extended `tests/architecture/one-model-option-source.test.ts` population. RECENTS AT EXACTLY 3 MOUNTS, all editing `settings.defaultChatModel`: the top bar (trigger), the Settings chat-model field and the wizard's field — deliberately ABSENT at the other seven (fallback-chat, embedding, image, fallback-image, persona override, Idea Board `board.model`, and the canvas-chat sidebar, which the writer VERIFIED writes the session-only `useCanvasChatStore.setModelSelection`, not the global setting). The wizard field lives on the EXISTING `openrouter` step (`id=wizard-chat-model`), writes the same setting, and the step's completion signal stays the saved key — no new step or detection. THE WRITER'S FOUR BRIEF CORRECTIONS, accepted: (1) `ModelInput` had EIGHT mounts, not nine — nine is the model-picking INSTANCE population once row 193's top-bar picker is counted; (2) `canBrowse` is ONE required boolean with no per-variant probe (the Settings card's `|| test.kind === 'ok'` was provably redundant because `testKey` saves before testing); (3) the field variant's browse button is no longer DISABLED without a key, because the panel must OPEN to say why (which the loud no-key requirement demands); (4) the recents RECORDING surface widened from the top-bar picker alone to the widget's three global-chat mounts (same ordering rule and same repo seam; the run funnel unchanged) so a choose at the Settings or wizard field moves the model to the front | docs=docs/17 row 199; docs/05 §Top bar/§Settings/§Onboarding; docs/18 §2.1 + §2.3; docs/08 matrix + the row-199 section with the arm table)
LANDED | row=197 | sha=7bda5ac | verify=MY OWN: gate GREEN 327/327 files · 4227 tests · combined peak 2320MB of the 3000MB cap (raw log /tmp/gate-verify-197.out) PLUS my own injection with every hash printed: A untouched GREEN 4 files / 127 tests (runEngine 0447ac33e0f46251190fa853e1a2cea58a255c7d, entity-batch b5378a172873dfc13e477921080636afa97c6ea3 — BYTE-IDENTICAL to the hashes the writer reported as its own baseline); B the STRUCTURED hint no longer wins over the brief regex — runEngine 083091b8af254306be30aa077fd0c62e4bfa60df → RED 1 on the NAMED pin 'the structured level hint WINS over a conflicting `level N` sentence in the brief' (my FIRST B run was green because my chosen test set did not include `tests/llm/runEngine.test.ts`, where that pin lives — MY selection error, recorded, NOT a missing pin; re-run on the right file reds exactly there); C the unmatched-hint report suppressed — entity-batch 6b27759ea408d6b1087a1b3de29e5b374b3c89c6 → RED 1 on 'reports the recording name and level, and never invents an entity for it'; restored byte-identically after every arm, tree clean | retired=branch feat/entity-hints (tip 7bda5ac, 0 commits outside main) + worktree /tmp/campaigner-entity-hints + writer session, after this verification | note=THE OWNER'S SYMPTOM IS CLOSED: a level stated in the module's own text now survives into the entity generator. The hint is `moduleEntityKindSchema.levelHint` (int 1..20, additive, LOUD zod failure) on the EXISTING per-entity record BESIDE `intent` (row 141) and `bestiary` (row 107) — my brief's top-level `entityHints` array was DECLINED with the reason stated (a parallel array would be a second name-keyed per-entity record/cap/carry/comparison, AGENTS rule 4) and so was my 'the batch target type gains the hint' (the by-name read is the `entityIntentFor` precedent). The SPINE pass emits it (the pass that writes the premise, where the owner's gnome lived; the parts pass authors markdown only and CANNOT emit a structured record; the normalization pass CARRIES the spine's hint onto canonical records via `withEntityBestiarySlots`, two different levels throwing loudly, so nothing clobbers); the batch reads it by name and passes it BOTH as a brief paragraph and as `StartRunInput.entityLevelHint` persisted on the run row (reconstructed by resume/retry); `runStatblock` prefers the structured hint over the `/level\s*(\d{1,2})/i` regex with the regex kept ONLY as the no-hint fallback, and a printed level that differs gets the step's existing `notice` naming both levels (the NPC lane has no substitution contract); the entity panel shows a read-only `entity-level-hint` badge. An unmatched hint (a name the module's text never mentions) is a LOUD named issue via `domain/module.unmatchedEntityLevelHints` — never an invented entity. NAMED BOUNDARIES / UNPROVEN, stated by the writer rather than hidden: the encounter generator's own `encounterPartyLevel` is deliberately NOT re-wired this slice; and NO live-provider run was made — every pin mocks the transport/engine boundary, so the WIRING AND BYTES are proved, not a real model's obedience (the deviation notice is a notice, not a refusal). MY BRIEF WRONG ON DOCS: docs/07 is the M3 milestone, NOT the module designer spec — the writer amended docs/08-MODULE-DESIGNER.md instead | docs=docs/17 row 197; docs/18 §2.2 (new seam row after the intent row); docs/08-MODULE-DESIGNER §M4-C ('Entity level hint'); docs/08-TESTING (new section + INJECTION-PROVEN table)
LANDED | row=196 | sha=88cce10 | verify=MY OWN: gate GREEN 325/325 files · 4195 tests · combined peak 2311MB of the 3000MB cap (raw log /tmp/gate-verify-196.out) PLUS my own injection with every hash printed: A untouched GREEN 3 files / 118 tests (post-run-extras ce2431197b152a7319ddd31a0e9bc187ae0b28fe, mob-portrait-queue 1508030e768088f72175627c27770460daca0b17 — BYTE-IDENTICAL to the hashes the writer reported as its own baseline); B the completion trigger's fill call removed — post-run-extras 14140a6d308b5c52008dad074490ca352356379f → RED 3 = exactly the NAMED pins P1 'the FINAL restocked roster is illustrated by the completion trigger (revert-proof)', P2 'DIFFERENTIAL — the module trigger and the editor plan agree over the same post-restock state' and P3 'the fill is idempotent: a second pass and the sweep enqueue nothing for imaged kinds'; C the owning module's mob-image switch ignored — post-run-extras e5fd1e5ad21b836bc60fde2e76e29d4b6edaa8d8 → RED 1 = 'the owning module switch OFF keeps the automatic roster portraits manual'; D the seam's INVENTED lane dropped — mob-portrait-queue 4cbe7f3810eb92dd737498e8c2f2103df0c1a58a → RED 3 = P1/P2/P3 (the fixture's restocked entries ride the invented lane, which is why the writer's cited-lane arm was VOID); restored byte-identically after every arm, tree clean | retired=branch feat/portrait-trigger (tip 88cce10, 0 commits outside main) + worktree /tmp/campaigner-portrait-trigger + writer session, after this verification | note=COPIES 3→1: `features/campaign/mob-portrait-queue.enqueueEncounterPortraitFill` now carries 'run BOTH encounter portrait lanes', consumed by the editor's fill press, the module sweep AND the new run-completion trigger — so 'which creature needs art' is one decision with three triggers, never a fourth spelling. THE FIX: `post-run-extras` already received the fresh artifact and both lanes but returned early when a run carried no `extras` (the Cartographer map job passes none), so the restocked roster got no portraits; the completion seam now fills them when the result artifact is an encounter whose OWNING module wants mob images (`autoGenerateMobImages`), respecting `settings.imagesEnabled` (with the existing loud skip only when the roster is non-empty) and the stop epoch. THREE DECISIONS, all recorded: (1) the sweep's `artifact.moduleId === module.id` OWNERSHIP filter is KEPT deliberately — it is the module's automation scope (same as `encountersNeedingMaps`, row 129) and the new trigger keys on the artifact's own `moduleId`, so no roster-landing run is skipped; a campaign-level encounter is deliberately NOT auto-filled because there is no module switch to ask, and its route stays the editor button and a run's ticked `mobPortraits` extra. (2) An EMPTY roster at snapshot time stays silent — nothing to illustrate is not a failure, and the images-disabled toast fires only for a non-empty roster; recorded as a named limitation. (3) The sweep's stub-roster portraits are RECORDED as known waste, NOT skipped: skipping depends on a restock that can fail or be off (`autoGenerateBattlemaps`), which would leave a permanently cover-less encounter; recurring identities are already imaged and skipped, which bounds it. MY BRIEF'S COORDINATES WERE WRONG and the writer re-derived them: the roster-replacement is `runEncounterFinalize` (`runEngine.ts:6428`, `resultArtifactId` `:6460`, stocking authorization `:4036-4042`), not the Smith draft finalize I cited; and the restocked creatures ride the INVENTED lane, not authored NPC artifacts (its C1 arm was VOID for exactly that reason and it recorded the VOID instead of counting it). IT ALSO FIXED A LATENT BARRIER RACE in row 193's `tests/features/model-picker.test.tsx` (asserting the trigger's model text right after `findByTestId` while the text comes from an async live query) — measured green with its tree stashed, red with it, green after one `waitFor` — the docs/17 row 132 discipline, and evidence that this class of false red is still live (see row 192) | docs=docs/17 row 196 (NEW, unioned with row 191 in docs/17 only — no renumber needed, disjointness proven by `git diff --name-only 6ef801b 88cce10`); docs/11 §Module generation integration; docs/18 §2 post-run-automation row; docs/08-TESTING (new section + the differential table)
LANDED | row=191 | sha=1ca10cd | verify=MY OWN: gate GREEN 325/325 files · 4191 tests · combined peak 2241MB of the 3000MB cap (raw log /tmp/gate-verify-191.out) PLUS my own injection with every hash printed: A untouched GREEN 4 files / 88 tests (spellHeightening beb315976b7f4e8975d8e0bac99a03028da84319, pf2e-foundry 70dec7e48c4da342d8bd15d8908fa9445f6f2898 — BYTE-IDENTICAL to the hashes the writer reported as its own baseline); B the focus TRAIT predicate forced false (the focus arm can never fire) — spellData 7caac6f0162c6521628e90a69e454e8819067d0c → RED 9 across all 4 files incl. the NAMED pins 'a FOCUS spell auto-heightens from the CASTER LEVEL — the caller picks no rank', 'a focus spell on a level-less mob is a LOUD named issue — never a default rank', 'carries a FOCUS item's source fixed auto rank — item first, else the casting entry'; C my own first attempt only PRINTED the focus-arm code and changed no bytes → recorded as a VOID probe, not evidence; C2 the source fixed `autoHeightenLevel` precedence replaced by the ceil rule — spellHeightening a76a2749ad13d43fd703543789549b5d49c13414 → RED 2 on exactly the two precedence pins ('lets a focus spell's source fixed autoHeightenLevel win over the caster level', 'prefers the source fixed autoHeightenLevel over the ceil rule'); restored byte-identically after every real arm, tree clean | retired=branch feat/focus-heighten (tip 1ca10cd, 0 commits outside main) + worktree /tmp/campaigner-focus-heighten + writer session, after this verification | note=COPIES 1: the focus trait signal is a new SINGLE-SITE predicate (`domain/spellData.spellTraitsAreFocus`, beside row 189's `spellTraitsAreCantrip` under the same source-scan discipline) and the rank derivation is `spellAtRank`'s ONE new `focus-auto` arm — its OWN provenance, never a silent reuse of `cantrip-auto`, because a cantrip and a focus spell are different rules and a chip's provenance line is where the owner sees which one ran. PRECEDENCE, upstream-faithful: an explicit caller `castRank` wins over everything; else the source's fixed rank (`autoHeightenLevel`, resolved AT IMPORT in upstream's own order — the item's `system.location.autoHeightenLevel`, else the `spellcastingEntry`'s `system.autoHeightenLevel.value`); else `clamp(ceil(casterLevel/2),1,10)`. A focus item is stamped with NO cast rank (upstream ignores `heightenedLevel` for it) while a ranked non-focus item keeps row 189's ratified `heightenedLevel ?? level.value` — row 189's behaviour is unchanged and pinned. FIXTURES, real and byte-for-byte with sha256 recorded: `tests/fixtures/packs/pf2e/lawbringer-warpriest.json` (upstream pathfinder-monster-core, sha256 d6daa2f0d5856e2eae767a64626d65dfc6d1fa477c89b4301a6659becca16cd4, 66187 bytes — level 5, 'Athletic Rush' rank-less, both entries' autoHeightenLevel null) and `tests/fixtures/spells/athletic-rush.json` (focus spell, sha256 f672e99b18aff88aabea8b33953a0c0d04c7dfcc8210b0ee4d4194af7637ed18, 1482 bytes); the foundry-pf2e lane digest was amended 2→3 and NAMED. THE WRITER'S THREE BRIEF CORRECTIONS, all accepted and recorded: (1) a focus-spell fixture lives in `tests/fixtures/spells/`, which the html-to-text LANES block does not enumerate, so only the bestiary lane count moved and `foundry-pf2e-rules` did NOT; (2) my pin 'a focus spell on a level-less mob is loud' holds only for the CASTER-LEVEL arm — upstream short-circuits to a fixed `autoHeightenLevel` without needing the actor level, and it implemented that faithfully; (3) `autoHeightenLevel` cannot be read at rule time (not on `SpellData`), so the IMPORTER carries it — and a CANTRIP's own `autoHeightenLevel`, which upstream would also honour, is deliberately NOT read because the brief forbids touching the cantrip rule: a bounded deviation from upstream, recorded rather than hidden | docs=docs/17 row 191; docs/11 (the focus arm in the mob-spells spec); docs/12 §5 mapping row + §9; docs/18 §2 rows 105/106/209/217; docs/08 (row-191 section + the amended row-189 closing note)
LANDED | row=193 | sha=62c0cf9 | verify=MY OWN: gate GREEN 325/325 files · 4177 tests · combined peak 2228MB of the 3000MB cap (raw log /tmp/gate-verify-193.out) PLUS my own injection with every hash printed: A untouched GREEN 4 files / 28 tests (settings e6be7c3ee683c5cf176a8644fc6fb288a95569f1, model-picker db15d36d8b11ab21689f14176f55727d4c2d7310 — BYTE-IDENTICAL to the hashes the writer reported as its own baseline); B the recents list no longer DEDUPLICATES — settings 57d7ea381b614083e305921be8ef2dc80697ee5c → RED 5 incl. the NAMED pins 're-using A moves it to the front exactly once (no duplicate anywhere)' and 'shows Recently used in stored recency order (never re-sorted)'; C the CAP removed — settings cf63d5dc2ec466b5cc428e639458123a9dc9648e → RED 2 (both cap pins); D my own first attempt only PRINTED the recording call sites and changed no bytes → recorded as a VOID probe, not evidence; D2 the picker's recording call removed — model-picker ecd176ab4cf064b846782fe5795dcb18f2c00400 → RED 4 incl. 'picking writes the setting AND lands the model at the front'; restored byte-identically after every real arm, tree clean | retired=branch feat/model-picker (tip 62c0cf9, 0 commits outside main) + worktree /tmp/campaigner-model-picker + writer session, after this verification | note=COPIES 1→1: `features/settings/model-options.listModelIds` is the ONE account model-id option source (ModelInput's default browse list + the picker; the picker's would-be copied /models fetch is a seam call), the recents ORDER is one function (`domain/settings.withRecentChatModel`: move-to-front, dedupe, cap) and the RECORDING is one transactional repo function (`db/settingsRepo.recordRecentChatModel`). Cap = 8 (`RECENT_CHAT_MODELS_CAP`), oldest dropped. The picker is mounted in `TopBar` immediately beside Settings on every route, edits the SAME `defaultChatModel` the Settings field edits (docs/05 says so), keeps FREE-FORM entry (a typed id outside the account list is settable) and is LOUD when the list cannot load (a visible panel state AND a toast; the no-key state says WHY while recents and typed ids still work). 'USED' = the global first-try model in play, recorded at exactly TWO sites: the picker's own choose (so the list answers the pick just made) and `runEngine.executeFrom` — the file's documented single funnel — when `resolveChatModel(...) === settings.defaultChatModel`. Deliberately NOT recorded: image-mode runs, persona overrides, `fallbackChatModel`, image models, and the Settings field's own onChange (it is the SHARED `ModelInput` the fallback/image/embedding fields use, so hooking it generically would record forbidden tiers). NAMED LIMITATION (recorded, not hidden — a candidate small follow-up): module generation, canvas chat and the Idea Board resolve the global model OUTSIDE the run engine and are NOT recorded, so a model set in Settings (rather than chosen in the picker) and used only there will not appear in recents; anything the owner PICKS in the picker does land. THE WRITER'S OWN GATE CAUGHT A REAL BUG in its first version: awaiting the recording at `executeFrom`'s top held the run row's previous status long enough for a racing approve to observe a stale `awaiting_user` and start the SAME statblock step twice → RED in `module-gen-and-provenance` and `encounter-map-queue`; it fixed the recording to fire-and-forget (the method still catches and toasts its own failures). BRIEF CORRECTIONS ACCEPTED: my pin 6 as literally worded ('a second listModels call site cannot appear in src/') was INFEASIBLE because two legitimate consumers already exist (the Settings Test-key probe and ReasoningEffortSelect's reasoning metadata), so the scan pins the EXACT call-site population instead and still reds on a new one; and its new top-bar control legitimately names 'Chat model: …', which forced ONE existing pin's page-wide `button {name:/chat/i}` query (canvas-chat-thread, ledger 91) to be scoped to `<main>` — its row-inventory half unchanged | docs=docs/17 row 193; docs/05 §Top bar + §Settings; docs/18 §2.1 (recording seam) + §2.3 (picker and option source); docs/08 (matrix + a section with the differential arms); vite.config.ts nodeTestGlobs
LANDED | row=190 | sha=baebe9c | verify=MY OWN: gate GREEN 321/321 files · 4147 tests · combined peak 2282MB of the 3000MB cap (raw log /tmp/gate-verify-190.out) PLUS my own FOUR-ARM injection with every hash printed, EVERY arm RED on named pins and the tree restored byte-identically: A untouched GREEN 5 files / 89 tests (moduleDifficulty 357b036b0b00d023a735138d3ef3d0b407227157, roomBudget 92de773c6344f2610e11de473f3619d0f6f33b63 — BYTE-IDENTICAL to the hashes the writer reported as its own base); B a legacy row (no field) resolving to 'harder' instead of normal — moduleDifficulty d24f5bc657691f7cd1ca1c4d3d0b487ec16ec391 → RED 6 incl. 'reads a legacy module row (no field) as normal and reproduces today's numbers' and 'resolves a recorded difficulty verbatim and a legacy/absent one to normal'; C the encounter brief no longer stating the module difficulty — roomBudget a98ee21b0eab67798e0e3b766fc44679ca6cb22c → RED 3 incl. 'states the module's recorded difficulty in the prompt the model sees (docs/17 row 190)' and 'ships NO number under a verbatim policy: difficulty is a direction there' (the verbatim/licensing pin is REAL, not prose); D the ladder made non-monotone ('much-easier' set to 1×) — moduleDifficulty 064c9dd4b70a6ecddb454afbc9bb51926b859165 → RED 3 incl. 'documents a monotone ladder that is 1× at the middle and halves/doubles at the ends' and 'moves the budget monotonically for BOTH systems through roomBudgetBandUpperFor' | retired=branch feat/module-difficulty (tip baebe9c, 0 commits outside main) + worktree /tmp/campaigner-module-difficulty + writer session, after this verification | note=COPIES 1 per the writer: `domain/moduleDifficulty` is the ONE difficulty seam (5-step enum, label map, ladder, resolver) and `roomBudget.roomBudgetBandUpperFor` is the ONE place the multiplier scales a budget for both systems, held by the NEW SOURCE SCAN `tests/architecture/module-difficulty-seam.test.ts`. LADDER 0.5 / 0.75 / 1 / 1.5 / 2 — middle is exactly today's numbers, the ends exactly halve/double, every factor a round number so the scaled band stays legible, and it scales only Campaigner's OWN approximations (dnd5e `T+2`, pf2e `2 × party level`): NO Paizo GM Core or DMG number is embedded, quoted or paraphrased (docs/11 D12). SIBLING, NOT RIVAL, of row 180's policy — the policy decides WHICH rule bounds a room's challenge, difficulty decides HOW HARD the module is for this group and scales whatever numeric budget that rule produces; the two are independent and an owner may hold 'verbatim' + 'much-harder' at once. Under 'verbatim' NO number is computed, so the multiplier has nothing to move and difficulty reaches the model as a DIRECTION ONLY, pinned (my arm C). A module row that predates the field resolves to normal and produces byte-identical numbers (my arm B). The setting is chosen in the New Module dialog's 5-step control with the MIDDLE defaulted, carried in `newModuleDraftSchema` and stamped on the row at creation; resolved once per run at the same three sites the policy is. CREATION-ONLY by dispatch — an editable-after-creation control is deliberately NOT built, and the writer flagged no need for one. PROCESS NOTE: the writer's FIRST gate was RED (6 forbidden non-null assertions in its new tests) and it fixed and re-gated; its arm-D suggestion from my brief would have been VOID as specified (the dialog's prefill effect resets a `useState` initial value), so it injected the RENDERED default instead — the same void-probe discipline row 178 established | docs=docs/17 row 190; docs/11 (new 'D12 amendment — the module difficulty setting' + the D12 header line); docs/05 §New Module dialog; docs/08-MODULE-DESIGNER §Creation UI; docs/18 §2 (per-room budget row + NOT-Z); docs/08-TESTING (new difficulty section)
LANDED | row=188 | sha=9cc3cd5 | verify=MY OWN: gate GREEN 319/319 files · 4130 tests · combined peak 2407MB of the 3000MB cap (raw log /tmp/gate-verify-188.out) PLUS my own injection with every hash printed: A untouched GREEN 4 files / 81 tests (modulePdf 9d567fd088acd0cc79d510243fb3ba597c83050d, documentPlan ca62944884dab370796176815c7f0d1d7826a050 — BYTE-IDENTICAL to the hashes the writer reported as its own base); B the unknown-companion check disabled (a silent skip) — documentPlan b566c4db0c227307ad5bff4fdfd2d718d00df3b4 → RED 1 on the NAMED pin 'reports an UNKNOWN companion id by name — a failure of the WHOLE plan, never a skip'; C the companion's name prefix removed — modulePdf 8a7cd1884a86978fc5eba6326adae93ada26cfa6 → RED 2 on the NAMED pins 'keeps an artifact-sourced section's own mechanics AND gains its companion (additive)' and 'prints an introduced NPC's profile in a PART-sourced section's sidebar'; D my own fourth arm was mis-designed (the script only PRINTED the call-site count and changed no bytes — hash unchanged) → recorded as a VOID probe, NOT a green result; restored byte-identically after every real arm, tree clean | retired=branch feat/npc-sidebar (tip 9cc3cd5, 0 commits outside main) + worktree /tmp/campaigner-npc-sidebar + writer session, after this verification | note=COPIES 1 per the writer: `companionDetail` delegates to the SAME `roleDetail`/`companionContent`/`artifactDetail` seam row 187 built and adds only the row's own name (the only companion builders); `lib/pdfPageModel` UNTOUCHED — the placement ladder stays the renderer's. A plan section may name ONE `companion` (`documentPlanSectionSchema.companion`, `.nullish()` and NEVER a default, so every stored plan keeps parsing and a no-companion section materializes no key → byte-identical rendering); an unknown id, an ENCOUNTER companion (its roster/tactics/treasure/map are §4 own-page material and `detailPlacement` reads the SECTION's kind, so it would evade the own-page arm) and a companion that IS the section's own source are all LOUD failures of the whole plan, each with its reason. The planner prompt states the sidebar is SCARCE and that choosing WHICH introductions earn one is its own judgement call (the owner's delegation, verbatim in the row), and the plan dialog DISPLAYS each section's companion so the call can be corrected. `event` companions are deliberately allowed (measured §5 placement; their art still triggers §4 through `hasImage`) — recorded as a decision, not a proof. THE WRITER'S DEVIATION, accepted as better than my brief: appending only the row's `artifactDetail` produced an UNNAMED sidebar profile (an NPC with no stat block printed no name at all), so `companionDetail` prefixes the row's own name at the head of the MECHANICS half — which puts the name under §10.1's once-rule and the read-aloud/aside role gate WITH the profile it names | docs=docs/17 row 188; docs/07 §M3-D (plan fields + Printed/Read-from row + surface); docs/19 §4 + §5; docs/18 §2.3 (plan-row pointer + a NEW companion seam row + the §7 row extension); docs/08 (matrix + arm table + gate numbers)
LANDED | row=187 | sha=d51be50 | verify=MY OWN: gate GREEN 319/319 files · 4120 tests · combined peak 2329MB of the 3000MB cap (raw log /tmp/gate-verify-187.out) PLUS my own FOUR-ARM injection with every hash printed: A untouched GREEN 3 files / 61 tests, modulePdf fdf6de35e18a87867eab684c861806765c8598ea — BYTE-IDENTICAL to the hash the writer reported for its landed file, so the union landed as authorized; B (first attempt) a malformed injection of my own (invalid `...[] as Content[]` syntax) → recorded as a VOID probe, not evidence; B2 the companion's artwork half removed — modulePdf 9560847c6687b71a3d379020900396af3edd93af → RED 11 incl. the NAMED pins 'prints an artifact's OWN image wherever it is described, and the plan's anchors as EXTRAS', 'prints the artifact's OWN cover and map plate in a PLANNED document with no anchors at all', 'prints the NPC gallery's portrait — the gallery is where an NPC is described', 'is LOUD when a planned section's own image exists but is not in the preloaded set'; C a planned section's `hasImage` back to plan-anchors only — 1fd47c14a4d5516e658efc83407141b5a8bde2b3 → RED 2 on those same two own-image pins (so the placement half IS pinned, not assumed); D imageInventories drops an encounter's own map plate — f4c99039401b2b7ab9fba258744d352fb3aea0a9 → RED 19 incl. 'pins the images at the budgets their own sites print at'; restored byte-identically, tree clean | retired=branch feat/pdf-images (tip d51be50, 0 commits outside main) + worktree /tmp/campaigner-pdf-images + writer session, after this verification | note=COPIES 2→1: `modulePdf.artifactOwnImages` is the ONE derivation of an artifact's own images — the placement decision's `hasImage`, the plan-anchor filter (an anchor naming the artifact's own picture is SKIPPED so nothing prints twice) and `imageInventories`' artifact half — and the preloader's SECOND 'what the document wants' filter was DELETED, not extended. `artifactDetail` answers in TWO halves (`artwork` prints at every description whatever the role; `mechanics` is the §10.1-once and read-aloud/aside-gated half) composed in ONE place (`companionContent`); the NPC gallery now prints portraits (its 'no cover thumbnails' comment was a false claim); a role governs text/mechanics, never the artifact's own picture. MY BRIEF WAS WRONG on rule (e): `imageInventories` DID collect every scoped artifact's own image, but `buildModulePdf` NARROWED the request set to `resolveDocumentPlan`'s anchors + the module cover, so an UNANCHORED own image was never preloaded and would have been reported as 'it was not in the preloaded image set' the moment the renderer printed it — deleting that narrowing is the defect's OTHER HALF, and the loaded set and printed set are now one decision. THE UNION: my disjointness miss (row 187 briefed against 177d01b while row 184's follow-up was in flight in `modulePdf.ts`) produced the ONE non-docs conflict; the writer ABORTED and reported per AGENTS §Parallel writers 3, I READ BOTH SIDES, authorized the mechanical union (184's `spellIndexes` line + 187's `requests = imageInventories(...).requests`), and required the full re-gate (319/4120 = base 319/4115 + its 5 pins) plus a focused run keeping row 184's `mob-spells-pdf` pin green through the union | docs=docs/17 row 187; docs/19 §2 + BUILD STATE + §11 step 7 (the owner-DEFERRED print refinement stays deferred); docs/18 §2.3 rows 232/234; docs/07 §M3-D table; docs/08 matrix + a row-187 section with the arm table and the union numbers
LANDED | row=184 | sha=faa704e | verify=MY OWN, on top of the writer's TWO rounds: after its first landing I REBASED it (it had gone quiet before pushing), gated it myself (GREEN 318/4104, peak 2316MB) and ran a FOUR-ARM injection — untouched GREEN 34/34; the unresolved-name issue list emptied → RED 4 (incl. 'an invented spell name stays an UNRESOLVED entry and is loud, never dropped'); the cantrip rule no longer fed casterLevel → RED 3; and the corpus read's SYSTEM FILTER DROPPED → file hash changed and ALL FIVE FILES STAYED GREEN, a REAL GAP (the new seam had no pin of its own: `grep -rln "loadSpellChunksFor|statBlockSystems|loadSpellIndexesFor" tests/` returned nothing). Sent back for the pin + a layering judgement; it returned a second round which I REBASED onto 177d01b (union on docs/17, auto-merge on lib/modulePdf verified to keep BOTH lands' halves), gated MYSELF (GREEN 319/4115 tests, peak 2296MB) and re-injected: the SAME system-filter arm now RED 2 ('returns only the asked system's ready spell chunks — in BOTH directions', 'indexes ONLY the systems the build's blocks carry'), and a silent fallback in the assignment contract (a ranked spell with no cast rank cast at rank 1) RED 1. The pushed SHA differs from my gated one by EXACTLY one file, `docs/20-ORCHESTRATION.md` (3 insert/2 delete), PROVEN: `git diff --name-only d3d12ce faa704e -- src tests vite.config.ts` returns ZERO paths, so the gated bytes are the pushed bytes | retired=branch feat/mob-spells (tip faa704e, 0 commits outside main) + worktree /tmp/campaigner-mob-spells + writer session, after this verification | note=COPIES 1 per the writer: `spellAtRank` stays the ONE heightening rule (a chip computes nothing itself), one `components/spell-chip.SpellChip`, one comparable-name form, one corpus read (`db/spellRepo.loadSpellChunksFor` = `rulebookRepo.readyBookIds` + `chunkRepo.listChunksByType('spell')`), one resolver (`domain/mobSpells.mobSpellChips`) asked by the run boundary, the stat-block card AND both PDF exporters. THE OWNER'S NO-INVENTION POLICY IS ENFORCED AT THE BOUNDARY on BOTH AI-authored mob paths (NPC stat-block step, encounter inline monsters): one repair turn naming the offenders, then the entry is STORED so its chip renders UNRESOLVED with the name visible, with the raw `spellIssues` on the step output AND an amber notice naming spell and mob — never dropped, never folded into prose. Heightening: chip `title` and the printed PDF line are the same `mobSpellChipDetail` bytes (applied rank, provenance, warnings; prose-only prints its note verbatim with no invented number); a cantrip's rank is derived by `spellAtRank` from the mob's own printed level, pinned with a level-5/level-9 differential so a caller cannot compute it. THE WRITER'S OWN GATE CAUGHT A REAL LAYERING BUG and folded it: `db/spellRepo` importing the `@/search` barrel broke 3 LLM tests that mock it with only `searchRules`; `readyBookIds` moved down to `db/rulebookRepo` (re-exported by `@/search`, every caller unchanged) with the two component copies of the ready filter pinned as ONE (`tests/db/ready-book-seam.test.ts`). In round two it also moved the pure `spellCorpusEntries` projection from `features/spells/spell-rows` DOWN to `domain/spellData` (feature re-exports), so this arc has no db/llm → feature edge left. STILL QUEUED (row 189): the STANDARD/library-mob half — the pf2e bestiary importer must stamp `spells` on a library stat block from the creature's own data (the schema already carries the field, so chips render for free), with the SAME loud-unresolved policy for an importer-written name, a pack re-import, importer tests and the docs/12 row | docs=docs/17 row 184; docs/11 mob-spells spec; docs/18 §2.1/§2.2/§2.3 + NOT-Z arms; docs/08 (matrix + the three injected arms)
LANDED | row=186 | sha=177d01b | verify=MY OWN: gate GREEN 312/312 files · 4072 tests · combined peak 2358MB of the 3000MB cap (raw log /tmp/gate-verify-186.out) PLUS my own THREE-ARM injection with every hash printed: A untouched GREEN 2 files / 54 tests (pdfPageModel 1e8883daaab0437e7fae0244d5cff8fcf849ec64, modulePdf 83b652fa781d25e874f6ade0219c43ff51eb6979 — byte-identical to the hashes the writer reported as its own base); B `isMarkerContent` forced false (a marker counted as real content) — pdfPageModel 2455f20889f7c930f63a9886d8914599d901709c → RED 3: 'keeps the own-page artifact's details AND prints no page that is only the announcement', 'never emits a page whose whole content is the own-page announcement (two own-page artifacts in a row)', 'renders a page whose sidebar carries no REAL companion content as ONE full-width stack'; C `pageNodes` back to the pre-186 `sidebar.length === 0` test — modulePdf 4aec22d39a416ce46dcfcb41def0ac4c59610605 → RED 1 'prints a companion-only page as ONE full-width stack, never as an empty column beside a populated one' (one red, not two, because the writer ALSO moved markers out of the sidebar — the marker-only-sidebar case is arm B's to catch, and the writer's own arm D caught both together with 3 reds); restored byte-identically, tree clean | retired=branch feat/pdf-layout (tip 177d01b, 0 commits outside main) + worktree /tmp/campaigner-pdf-layout + writer session, after this verification | note=ONE rule, ONE seam: `pdfPageModel.marker()` is the ONE constructor for all three marker sentences and BRANDS the node with a Symbol (so the definition JSON and every byte-determinism pin are untouched), `isMarkerContent()` is the ONE predicate and NEITHER caller matches prose; a page lays out as ONE full-width column unless BOTH halves carry real content, markers ride the TEXT column and can never form a page (the paginator's `flush` drops a page whose whole content would be markers), and a companion-only carry page is KEPT and printed full width in the detail tier — nothing clipped (docs/19 §9). MY BRIEF WAS WRONG ON THE MECHANISM and the writer corrected it with evidence: I claimed the comical page came from a chapter-start's empty main column; in the procedural document that page holds the chapter heading, so the pointer still prints — the genuinely empty page needs an adjacent block after an own page (measured on repeat-planned p5). And the owner's CONFIRMED narrow-main page was a marker-only NON-EMPTY sidebar, not the mirror of the empty-main case: the writer enumerated FOUR such pages pre-fix (large-procedural p7/p9/p11, large-planned p3 with 6 real main nodes beside a one-sentence sidebar, repeat-planned p3) and eliminated the alternatives (no sidebar is non-empty yet renders nothing; no page has an empty sidebar ARRAY with a narrow main, which would have contradicted pageNodes). Its arm C as I briefed it was a VOID probe (identical bytes → no evidence) and is recorded as VOID, not as a red. 7 new pins + 2 updated in place + a FIFTH fixture (pdfLayoutCarryFixture), including a RENDERED pdfjs pin that reads item transform[4] to prove the announcement starts at the page margin instead of the sidebar offset; content baseline NOT regenerated | docs=docs/17 row 186; docs/19 §3/§5/§11 + build-state; docs/18 §2; docs/08 matrix + a row-186 section carrying the per-page enumeration and the arms table
LANDED | row=185 | sha=cbcfbc3 | verify=MY OWN: gate GREEN 312/312 files · 4065 tests (my raw log /tmp/gate-verify-185.out, diff base 5d7da95) PLUS my own THREE-ARM injection with every hash printed and the tree proven clean after restore: A untouched GREEN 2 files / 32 tests (button 857b2bb44ce2715e4407c41169cc245f821f942f, reader fdf84659480aaa326945b7bf59ba775d0b04ee43); B a SECOND spelling of the audience label ('GM document — premise, part plan, parts, artifacts, maps, gallery, treasure') injected into the reader — reader 0d5340178c0b304bb105e990a85863594e5fb219 → RED 1 on the NAMED pin 'declares each audience label exactly once' (the centralization claim: a copied menu cannot hide); C the reader's `<ModulePdfButton>` mount removed — reader 1e78b2f2af1c5605522a219451bfe034c177fd1f → RED 2 on the NAMED pins 'mounts the ONE control from the three module surfaces' + 'reader header carries the ONE module-PDF control'; restored byte-identically (reader fdf84659…), `git status` clean | retired=branch feat/reader-pdf (tip cbcfbc3, 0 commits outside main) + worktree /tmp/campaigner-reader-pdf + writer session, all after this verification | note=COPIES 1 — the export control stays the ONE `features/modules/module-pdf-button.tsx`, now mounted from THREE module surfaces (canvas header, campaign-tree module group, reader header) with the reader's own `readerArtifacts` pool. NEW `tests/architecture/module-pdf-seam.test.ts` pins the label population (each audience label declared in exactly ONE file under src/) AND the mount population (exactly the three surfaces), so a fourth copy or a forked menu reds; the reader behaviour pin requires exactly TWO menuitems. WRITER'S BRIEF CORRECTION, accepted: my 'keep ml-auto on Battle table and put the new control first' could not both hold — the anchor moved onto a wrapper around the new first control and nothing else in the cluster changed. Its needle finding is real too: a bare 'GM document' is NOT unique under src/ (`lib/modulePdf.ts` prose says 'the GM document'), so the seam pin uses the full em-dash labels. THE ASYMMETRY IS STATED, not implicit: `ModulePlanButton` deliberately does NOT mount on the reader (the export is the module's ACTION on all three surfaces; the plan is an INSPECTOR of what the last export decided) and docs/05, docs/18 §2 and docs/17 row 185 say so. Existing `canvas-chat-thread.test.tsx` exactly-ONE canvas-destination pin verified unmodified and green | docs=docs/17 row 185; docs/05 (reader header + printing section); docs/18 §2 mount list; docs/08-TESTING matrix; docs/07 §The export surface; docs/08-MODULE-DESIGNER reader nav
LANDED | row=183 | sha=c37e4de | verify=my gate GREEN 311/311 files · 4062 tests · combined peak 2290MB of the 3000MB cap, PLUS my own TWO-ARM differential on the rule with every hash printed: A unmodified GREEN 33/33 (9c4c37a5…); B cantrip rank ceil→floor RED 6 (2d656f88…); C interval step floor→round RED 7 (98004e1c…); restored byte-identically | retired=branch feat/spell-heightening + worktree /tmp/campaigner-heightening with this landing | note=COPIES 1: `domain/spellHeightening.spellAtRank` is the ONE heightening rule (fixed = highest listed layer ≤ rank; interval = the source DELTA per floor((rank−rulesBase)/interval) step + per-step area add; cantrip = clamp(ceil(casterLevel/2),1,10) owned by the rule; prose-only = the applicable note VERBATIM + loud marker, never a computed number; formulas combine symbolically and are never rolled). THE WRITER CLOSED A REAL GAP IN MY BRIEF: row 181's payload lacked base damage/area, so an interval spell would have computed its delta as the total (Fireball 2d6 instead of 8d6) — it extended the SAME payload (additive, emitted text byte-identical) and amended row 181's pins. TWO DEVIATIONS ACCEPTED as better than my brief: the request is {castRank?, casterLevel?} because one number cannot express "rank R by a level-L caster" without the caller re-implementing the cantrip rule; and a non-whole interval step is FLOORED with appliedSteps/stepRemainder + a named warning instead of throwing, because a (+2) spell cast one rank up is a legal cast that gains nothing (pinned against Foundry's own reference implementation). REBASE NOTE (my disjointness miss, resolved mechanically): row 182 and row 183 both amended docs/08 and vite.config's nodeTestGlobs — the config entries merged automatically and docs/08 was resolved as a union; both ledger rows are present and no slice was renumbered
LANDED | row=182 | sha=e97076e | verify=my gate GREEN 309/309 files · 4029 tests · combined peak 2301MB of the 3000MB cap, PLUS my own THREE-ARM differential with every arm's file hash printed: A unmodified GREEN 22/22 (routes 5cc58982… / creatureRepo 50ca1e96…); B the `campaignIdFromPath` spells arm removed RED 1 (routes f692c383…); C one folded read site reverted to a raw `where('chunkType')` RED 3 — the source scan fires (creatureRepo 78b3ea12…); restored byte-identically | retired=branch feat/spells-page + worktree /tmp/campaigner-spells-page with this landing | note=COPIES 3→1: `chunkRepo.listChunksByType` is the ONE chunk-type read (two raw sites folded, source-scan pinned); the chip element is shared by wiki chips and the spell list (`src/components/chip.tsx`, wiki HTML byte-identical); campaign-scoped route + campaignIdFromPath + tab + quick-find; rank sort (cantrip first), tradition MULTI-select, corrupt `spellData` row LOUD, per-system named empty states, Rules type filter gained `Spells` (the row-181 regression); the card shows the licence line from the chunk text and renders heightening VERBATIM pending row 183
LANDED | row=181 | sha=a8c5180 | verify=my gate GREEN 305/305 files · 4006 tests · combined peak 2364MB of the 3000MB cap, PLUS my own THREE-ARM differential on the ingest lane with every arm's file hash printed: A unmodified GREEN 23/23 (bc4265dc…); B the heightening capture emptied RED 2 (2bf3829e…); C the spell TEXT emission perturbed RED 3 — the byte-identity pin fires (aff15b26…); restored byte-identically | retired=branch feat/spell-data + worktree /tmp/campaigner-spell-data with this landing | note=COPIES 1: the ONE pf2e-rules field mapping emits text AND `spellData` (`src/domain/spellData.ts`) — rank/cantrip/traditions/traits/cast/heightening(verbatim)/heighteningEntries/heighteningUnparsed/publication, with the emitted TEXT byte-identical (four fixtures measured, pinned) because the text IS the contentHash. Reported surprises, all handled: the fixture carries FOUR fixed heightening entries (3/5/7/9), not three; the second htmlToText call reddened the stripper source-scan, amended 1→2 / 8→9 (still one stripper); and spells leaving `section` made the Rules type filter stop surfacing them — folded into row 182 with a pin
LANDED | row=180 | sha=31a4327 | verify=my gate GREEN 303/303 files · 3992 tests · combined peak 2267MB of the 3000MB cap, PLUS my own TWO-DIRECTIONAL differential on the resolver, each arm hash-printed and all three differing: A honoured → GREEN 66/66 (9742cf4f…); B forced `'system'` → RED 2 — the differential pin and the amended legacy-Cartographer pin (9d9e1add…); C forced `'pf2e-budget'` → RED 4 — the legacy/verbatim byte pins collapse (f2746cf9…); restored byte-identically | retired=branch feat/encounter-budget-policy + worktree /tmp/campaigner-budget-policy with this landing | note=COPIES 2→1: ONE resolved policy (`domain/encounterBudget.resolveEncounterBudgetPolicy` → `roomBudget.encounterBudgetFor`) threaded to cartographer/roster-sizing/repopulate/finalize, stamped on the module at creation and chosen in the New Module dialog; default = pathfinder2e → `pf2e-budget` (sensible), everything else `system`; legacy rows without the field read `system` and are byte-identical; `verbatim` stays selectable for the licensing stance. KNOWN SCOPE NOTE: the Smith draft prompt carries no room-budget clause (it has no rooms by design) — the policy reaches the room-sizing consumers
LANDED | row=179 | sha=9efb0a9 | verify=my gate GREEN 302/302 files · 3979 tests · combined peak 2302MB of the 3000MB cap, PLUS my own old-bypass injection RED 6/targeted — the encounter and ordinary card pins, the roster card pins, and the source scan all fail when `openArtifact` is suppressed — hash d050a55e… identical after restore | retired=branch feat/encounter-card-opening + worktree /tmp/campaigner-encounter-card with this landing | note=COPIES 2→1: ModuleReaderPage.openArtifact is the one opening seam; EncounterCard is the one encounter roster surface. Sidebar encounter rows now peek like every other artifact; inline roster mount removed; Run battle/resume and explicit Open in workspace preserved
LANDED | row=178 | sha=e90de32 | verify=MY OWN CORRECTED DIFFERENTIAL, with each arm's file hash printed so the arms demonstrably differs — delay-only with the cure INTACT **GREEN 5/5** (`flush.ts` 357a7d35…), the same delay with `actDrained` BYPASSED **RED 3/5**, three act() guard failures (`flush.ts` 28f0ae82…); the writer's own 3x features-chunk canary was green, and its 4x delay range (120/240/480ms) stayed green | retired=branch fix/act-flake + worktree /tmp/campaigner-flake with this landing | note=THE FLAKE IS CLOSED at the harness seam (`actDrained` on the file's two shared read helpers + two post-mount writes, no assertion touched, guard and setup.ts untouched). MY EARLIER "FALSIFICATION" WAS A VOID PROBE, and the record says so: my script broke `actDrained` in the SAME step as the delay injection, so both arms measured identical bytes and their identical RED was the tell I misread as evidence; the writer's reconciliation was right, and the fix had already passed my full gate (302/302) before that. Rule added to AGENTS §Host hygiene 7: a differential's arms must be shown to differ, and identical arms are a VOID probe, never evidence against a landing
LANDED | row=177 | sha=2083fdb | verify=my gate GREEN 302/302 files · 3978 tests (UNCHANGED while 33 files disappear) · combined peak 2334MB of the 3000MB cap, PLUS my own independent re-run of the merge accounting from `55be755` — `expect(` sites 1728->1728 and every cluster's test names identical (my count 393 vs the writer's 389 static: I count `it.each` as a name); the writer's own 3x canary was green | retired=branch feat/test-cluster-sweep + worktree /tmp/campaigner-sweep with this landing | note=OWNER IDEA, MEASURED TO ITS LIMIT: 39 files -> 6, framework cost -40.4s (features -27.8, llm -12.6) for 33 merged files, but the gate WALL moved only -9s (454s -> 445s) because the features pair runs concurrently and the wall is set by the LONGEST chunk, not the sum. THE DECISION: stop sweeping — the parallel gate already captured the wall benefit, further merging buys box load, and the remaining gate time is actual test time, which merging does not touch. Four NEW stop conditions measured: a merged file shares ONE mock instance per module (a file-level `beforeEach(vi.resetAllMocks)` is required); the node/jsdom project split is a HARD stop; one cluster was split back out after the merge CAUSED a sibling's `chat` count to change 2->3; direct-global mutators go LAST
LANDED | row=176 | sha=5e4e53e | verify=my gate GREEN 335/335 files · 3978 tests (the TEST COUNT IS UNCHANGED while 4 files disappear) · combined peak 2161MB of the 3000MB cap, PLUS my own INDEPENDENT ACCOUNTING — a merge's claim is "nothing was lost", so I counted it myself from `bbb10ea`: the five originals hold 32 test names and 116 `expect(` sites, the merged file holds 32 and 116 — identical — with the writer's one-process A/B showing −7.66s = 1.92s per removed file and a 3× sequential canary green | retired=branch feat/test-cluster-merge + worktree /tmp/campaigner-merge with this landing | note=OWNER IDEA, CONFIRMED: bundling tests that share one background is viable and measured; the sweep rule and its stop conditions are in row 176
LANDED | row=175 | sha=053aaa0 | verify=my gate GREEN with the NEW script (339/339 files, 3978 tests, 2230MB combined peak against the 3000MB cap) PLUS my own three probes, because a runner cannot be verified by a pin: (1) the diff-scoped PLAN reproduced on four synthetic edits — docs-only → `mode=docs-only` (vitest skipped), `src/lib` → full with `tests_lib` FIRST, `src/features` → full with both features halves FIRST, one test file → `mode=tests-only` running only its chunk; (2) the WATCHDOG KILL, forced with a 250MB cap on `tests/domain` → `WATCHDOG(hard)` killed it, VOID, not retried, GATE RED, NO orphan process and the lock released; (3) the isolate NO re-read in its raw log (10 of 14 files failing the same way across three runs — a deterministic mock leak, never adopted) | retired=branch feat/gate-wallclock + worktree /tmp/campaigner-gate with this landing | note=MEASURED: vitest phase 706.6s → 386s (total gate ~13min → 7m41s) with nothing dropped; at most two chunks at once, one worker each in its own `setsid` group; combined-RSS watchdog with a 90% soft fallback to sequential
LANDED | row=174 | sha=2ebabc5 | verify=my gate GREEN 339/339 files · 3978 tests · peak 1207MB of the 3000MB cap, lint 0, typecheck clean | retired=none (same-session follow-up; no writer, branch or worktree) | note=owner-reported layout defect on row 173's board; the diagnosis was CORRECTED against the installed package (the missing `theme="none"` was NOT the cause — @uiw uses `theme` only for a class name and CM's base theme sets no background), the real causes being the light-mode palette plus a `flex-1` root that never resolved in the shell's non-flex <main>; COPIES: 1 — checked, no duplication (the editor config is ONE named seam)
LANDED | row=173 | sha=4d7f528 | verify=my gate GREEN 338/338 files · 3975 tests · peak 1236MB of the 3000MB cap, lint 0, typecheck clean; the FIRST gate of this slice was RED (4 `db.verno` head pins + 4 knock-on console-noise failures from the aborted tests) and the re-gate is green after updating the pins and fixing the warning's CAUSE, never allowlisting it | retired=none — the dead writer (cf914bd5) had no branch and no worktree; its uncommitted slice was salvaged, re-verified and landed here, and the session is deleted | note=COPIES: 2→1 clipboard (two persona-panel call sites folded) + editor theme 2→1; the module canvas chat was deliberately NOT reused (module-shaped protocol) — the seams below it were
LANDED | row=172 | sha=109bc9b | verify=my gate GREEN 334/334 files · 3945 tests · peak 1230MB of the 3000MB cap (the writer's own numbers reproduced), PLUS the tripwire's self-proof on REAL change — after rebasing onto row 171 it went RED by itself with `STALE BASELINE ENTRY — 601a24cbc975d090 [the seven isRecord sites]`, exactly the mechanism it was built for; I deleted that one group (13 lines, nothing else) and it went green; then MY injection (a fresh copy of the document-record body appended to `packImport.ts`) RED with `NEW DUPLICATE — shared normalized body 601a24cbc975d090 (75 chars) is implemented at 2 sites` — the SAME hash the removed entry carried, so the detector sees the population it exists for | retired=branch feat/dup-tripwire + worktree /tmp/campaigner-dupes with this landing | note=COPIES: 1 — the detector slice folded nothing by design; its 16-group baseline is the measured debt inventory (see QUEUE)
LANDED | row=171 | sha=18165fa | verify=my gate GREEN 333/333 files · 3955 tests · peak 1221MB of the 3000MB cap, PLUS my injection (the unwrap made RECURSIVE, flattening nested arrays) RED 2/29 — `unwraps exactly ONE level — an element that is itself an array stays a document` and `N skipped finally means N` — hash b91e55f9… identical after restore | retired=branch feat/pack-array-docs + worktree /tmp/campaigner-arraydocs with this landing | note=OWNER DECISION A: COPIES 7→1 (`isDocumentRecord` in the ONE parse seam); the seven `isRecord` copies are gone, a top-level array is N documents in BOTH formats, an empty top-level array throws by name, and the skip count is per element
LANDED | row=170 | sha=302561c | verify=my gate GREEN 333/333 files · 3941 tests · peak 1233MB of the 3000MB cap, PLUS my injection (the `@`-target whitespace split made GENERIC, the "obvious" fix the rule scoping avoids) RED 4/46 — `space inside a uuid target: expected 'casts Peaceful on it' to be 'casts Peaceful Rest on it'` plus two lane digests — hash d2b9b769… identical after restore | retired=branch feat/ingest-notation + worktree /tmp/campaigner-notation with this landing | note=the writer died silently after committing, resumed on one nudge, then landed; its commit held code+tests+docs/17+18, its docs/08 delta was only committed at landing
LANDED | row=169 | sha=616ea98 | verify=my gate GREEN 333/333 files · 3936 tests · peak 1220MB of the 3000MB cap, PLUS my injection (a row whose stored fields are all empty made silently blank instead of named) RED 1/20 on `names a row whose stored fields are all empty instead of dropping it quietly`, hash 4e4a1165… identical after restore | retired=branch feat/planner-toolkit + worktree /tmp/campaigner-planner with this landing | note=docs/19 §11 slice 2; docs/18 §5's layout-debt paragraph amended (it still said v2 was "not built")
LANDED | row=168 | sha=730c9a7 | verify=my gate GREEN 333/333 files · 3930 tests · peak 1218MB of the 3000MB cap, PLUS my two injections — (1) the collision rule inverted (older row wins) RED 2/10 on `keeps the NEWER updatedAt…` + the tie-break pin; (2) the fold leaked into the `artifact:` key space RED 1/10 with the throw naming the key — each restored byte-identically (db.ts a026f437…, creature.ts a32e185a…) | retired=branch feat/persisted-key-fold + worktree /tmp/campaigner-keys-fold with this landing
LANDED | row=167 | sha=5348293 | verify=my gate GREEN 332/332 files · 3919 tests · peak 1249MB of the 3000MB cap, PLUS my injection (hand-rolled key restored in roomBudget.resolveBriefMonsterLevels) RED 2/18 — `expected [ undefined ] to deeply equal [ '2' ]` — file hash bf776416f1aafb3a15b73a0f1c1cf623b5a1c0dc identical after restore | retired=branch feat/name-key-spaces + worktree /tmp/campaigner-keys with this landing
```

Older landings are `docs/17`'s business — this section is not a history.

## Guards (why this workflow has teeth)

```
GUARD | failure=stale workers | where=plugin dsh-plugin-subagent-delete v0.2.0 (vendored ~/.dsh/profiles/web/vendor/sad, mounted in cordis.patch.yml) — the core provides send_message/interrupt_agent/list_agents, the plugin adds delete_subagent/release_subagent/list_subagents + web-UI removal | verify=spawn a trivial probe subagent, delete it, expect dirRemoved/projRemoved/removed_from_ui_list true (done 2026-09-15, DSH 0.1.5-rc.2) — RE-RUN THIS AFTER EVERY DSH UPDATE: the plugin is vendored (`file:./vendor/sad`) and its README targets DSH 0.1.0-rc.8, so an update can break the delete path while the tools still appear in the list
GUARD | failure=RAM / worker storms | where=scripts/gate.sh (atomic lock; at most TWO chunks at once, one worker each in its own `setsid` group; `tests/features` split so the long tails overlap; COMBINED-RSS watchdog at 3000MB with a 90% soft FALLBACK to sequential before the kill line; a VOID chunk re-run sequentially and never counted; a per-run union check that the chunk lists are exactly the test files, none twice; diff-scoped ORDER + exactly two skips — docs-only runs no vitest, tests-only runs only its chunk) + AGENTS §Host hygiene 1–7 | verify=`bash scripts/board.sh` (lock owner + orphans + load), `GATE_PLAN_ONLY=1 bash scripts/gate.sh` (prints the plan, runs nothing), and never hand-roll a gate
GUARD | failure=context / compaction | where=model route deepseek-official/deepseek-flash declares a 1,000,000-token context (native catalog); `compaction-basic` is patched in ~/.dsh/profiles/web/cordis.patch.yml (maxTokens 65536, compactionRetries 2, summarizer pinned to z-ai/glm-5.3-flash) because the 8192 default truncated large-span summaries and failed with "Compaction could not produce a useful summary" — the predecessor's unrecoverable state | verify=the compaction block is still in cordis.patch.yml; a CoS session keeps its thread small on purpose (AGENTS §Chief of staff, context budget)
```

## Recovery pointers

```
RECOVERY | context=/home/box/Harness/Tetris/recovered-campaigner-context.md | content=the Campaigner CoS, 2026-09-09→09-14, ~374 turns, 24 compaction summaries + 196 owner messages | note=the header names session-b25245f5 and the CivGlm session dir; the exact SOURCE SESSION IS NOT PINNED — neither that log (16.5MB, last write 19:37) nor the Campaigner dir's largest (session-e5adac67, 35MB, last write 19:14) reconciles with the content's last events (20:28–22:15) | status=content intact, header path unreliable
RECOVERY | context=/home/box/Harness/Tetris/recovered-civglm-context.md | content=the CivGlm CoS (the parallel project), ends ~2026-09-14 21:27 | note=references Campaigner only as a deploy-script example; not this project's session keeping
```
