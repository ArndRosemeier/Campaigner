{
  "note": "DEBT, NOT A LICENCE — and at docs/17 row 215 this population is EMPTY, which is a CLAIM the pin asserts rather than a blank file. The one cross-tree copy the union measurement found (hash 39d1cc194600176d: `publicationSourceLine` in `src/ingest/packs/pf2e-conditions.ts` and `src/ingest/packs/pf2e-rules.ts`, re-implemented as the `expected` reference in `tests/ingest/packs/source-line.test.ts`) was FOLDED in this slice: the test's reference now aliases the already-exported, already-imported production seam, so a pure formatter has exactly ONE implementation. A body that is implemented under `src/` AND under `tests/` reds `NEW DUPLICATE` naming both sites, so this population cannot grow silently; the non-vacuity arm seeds a `src/`-shaped and a `tests/`-shaped copy under a temp root and watches that arm fire. Never add an entry to make a new cross-tree copy green; baseline one only with a written reason and a named fold slice.",
  "scope": "ONE union scan: every `src/**/*.ts(x)` AND `tests/**/*.ts(x)` file in a single `scanRepo({ roots: ['src', 'tests'], exclude: ['tests/fixtures'] })`, compared only for the CROSS-TREE population — groups holding at least one site under `src/` AND at least one under `tests/`. `tests/fixtures/**` is excluded (captured upstream documents and prompt goldens repeat legitimately). It is the only exclusion, and it is a FORWARD GUARD rather than a load-bearing one at HEAD: `tests/fixtures/` holds ZERO `.ts`/`.tsx` files today (67 data files), so the union population is byte-identical with and without it — MEASURED while verifying row 215, after this slice's own note wrongly claimed the goldens would flood the population. Floor: 75 normalized characters, the SAME floor as the `src/` and test-tree inventories. See the test's doc comment for the normalization and what the tripwire cannot see (paraphrases and bodies under the floor).",
  "groups": []
}
